#include <stdio.h>

int main(void){
    int ar[5]= {1, 2, 3, 4, 5};
    int *p, a, *b, c;

    // endereço de um array vai ser sempre 6422280 + 4 x index ou vai ser um valor aleatório ?
    // dá pra acessar o elemento do array com o index fora do colchete ? 2[ar]

    p= &ar[1];




    return 0;
}
