#include <stdio.h>

int main(void){
    int T, N;

    scanf("%d", &T);

    while(scanf("%d", N) != EOF){
        int M[10001];
        int x, X[10001][61];
        int Q, op, x1, x2;

        for(int i=1; i<N; i++){
        
        }
    }


    return 0;
}